"use client";

// The admin panel is now a full page at /admin.
// This file is kept empty to avoid breaking imports; the dialog version
// has been replaced by <AdminPage /> at src/app/admin/page.tsx.
export {};
